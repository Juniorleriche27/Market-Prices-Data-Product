# Data folders

- `raw/` : raw data generated by the pipeline.
- `clean/` : cleaned data after basic quality checks.
- `product/` : final dataset used by the dashboard.

Sample CSV files are provided for reference.
